{
  "name": "albums",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/andraaspar/albums.git",
  "author": "András Parditka",
  "license": "MIT",
  "devDependencies": {
    "awesome-typescript-loader": "^3.2.2",
    "del-cli": "^1.1.0",
    "html-webpack-plugin": "^2.30.1",
    "node-sass": "^4.5.3",
    "sass-loader": "^6.0.6",
    "typescript": "^2.4.2",
    "webpack": "3.5.1"
  },
  "dependencies": {
    "express": "^4.15.4"
  },
  "scripts": {
    "installDeps": "yarn && cd src/scss && yarn && cd ../ts && yarn",
    "clean": "del-cli build",
    "compile": "yarn run installDeps && yarn run clean && webpack",
    "watch": "yarn run installDeps && yarn run clean && webpack --watch"
  }
}
